mutation RemoveJobForUser($userId: ID!, $jobId: ID!) {
    removeJob(userId: $userId, jobId: $jobId) {
        id
        company
        position
        city
        description
        dateRange {
            fromDate
            toDate
        }
        mode
    }
}
