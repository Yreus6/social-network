query GetJobForUser($userId: ID!, $jobId: ID!) {
    getJob(userId: $userId, jobId: $jobId) {
        id
        company
        position
        city
        description
        dateRange {
            fromDate
            toDate
        }
        mode
    }
}
