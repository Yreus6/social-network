query GetJobsForUser($userId: ID!) {
    getJobs(userId: $userId) {
        id
        company
        position
        city
        description
        isCurrentWork
        dateRange {
            fromDate
            toDate
        }
        mode
    }
}
