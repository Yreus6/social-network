query GetJobsForUser($userId: ID!) {
    getJobs(userId: $userId) {
        id
        company
        position
        city
        description
        dateRange {
            fromDate
            toDate
        }
        mode
    }
}
